# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# IDE / editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# NOTE: db.sqlite3 is intentionally NOT ignored — the pre-populated sample
# database is committed so the catalog works immediately on clone.
